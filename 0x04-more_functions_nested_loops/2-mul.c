#include "main.h"
/**
 * mul - multiplie two integers
 * @a: is the integers to be multiplied
 * @b: another integer
 * Return: product result
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
