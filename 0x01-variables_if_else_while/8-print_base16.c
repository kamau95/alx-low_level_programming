#include <stdio.h>

/**
 *main - main entry
 *
 *Return: always 0(success)
 */
int main(void)
{
	int c;
	char b;

	for (c = 0 ; c < 10 ; c++)
		putchar(a + '0');
	for (b = 'a' ; b <= 'f' ; j++)
		putchar(b);
	putchar('\n');
	return (0);
}
